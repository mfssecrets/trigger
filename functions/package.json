{
  "name": "trigger-functions",
  "description": "OTP email verification, username registry, and FCM push (Admin SDK — no client server key).",
  "engines": {
    "node": "20"
  },
  "main": "index.js",
  "scripts": {
    "serve": "firebase emulators:start --only functions,database",
    "deploy": "firebase deploy --only functions"
  },
  "dependencies": {
    "firebase-admin": "^12.7.0",
    "firebase-functions": "^5.1.1",
    "nodemailer": "^6.9.15"
  },
  "private": true
}
