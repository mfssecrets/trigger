package com.triggerapp.core.ui.theme

import androidx.compose.ui.graphics.Color

/**
 * Primary brand purple for buttons, links, and key accents in light/dynamic schemes.
 * @author udit
 */
val TriggerPurple = Color(0xFF6B4EE6)

/**
 * Darker purple for secondary emphasis and pressed states.
 * @author udit
 */
val TriggerPurpleDark = Color(0xFF4A32A8)

/**
 * Default app scaffold background (dark shell for chats and home).
 * @author udit
 */
val TriggerScreenBackground = Color(0xFF16191C)

/**
 * Elevated surface / muted panel fill on top of [TriggerScreenBackground].
 * @author udit
 */
val TriggerSurfaceMuted = Color(0xFF242325)

/**
 * Bright mint accent (online dots, sender bubbles, highlights).
 * @author udit
 */
val TriggerAccent = Color(0xFF63FFA3)

/**
 * Secondary mint variant for complementary highlights.
 * @author udit
 */
val TriggerAccentSecondary = Color(0xFF63FF90)
